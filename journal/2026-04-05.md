# Journal: 2026-04-05

## Session: NanoClaw infrastructure, todo system, test suite, upstream merge (Claude Code via Telegram)

### Corrections

- Proposed /api/message for todo assignment → Will questioned if it was the right pattern → Led to deeper analysis revealing message is outbound-only, delegation force-closes. Result: built /api/inject (synthetic inbound message). Learning: don't reach for the nearest tool — understand the flow semantics first.
- MC agent made UI changes when asked to add a todo about UI changes → Will: "It should just add the task." → Fixed by hardening the todo page WS prepend instruction. Learning: todo input must be strictly create-only, even if the agent has capability to execute.
- Said "let me do it" re: systemd restart → Will: "You should be able to restart the service though" → Was wrong about systemd access. Learning: try before saying you can't. XDG_RUNTIME_DIR was the fix.
- Proposed approach A/B/C for the todo system → Will pushed back on where todos should live, questioning whether NanoClaw should own them → Led to the right answer: MC owns todos, agents interact via MCP tools. Learning: Will challenges architectural assumptions. Present reasoning, not just options.

### Approvals

- "That feels right" — on single-list-grouped-by-horizon design (vs kanban)
- "Sounds good" — on brain dump chat-style input with conversational follow-up
- "I'm convinced" — on using /api/inject over delegation for todo assignments (after thorough reasoning)
- "This feels right" — on tome-observe immediate update tier for high-confidence signals

### Patterns

- **Deliberate decision-making on architecture**: Will doesn't accept the first proposal. He asks "what's the difference?", "is this really the right pattern?", "what's the risk?" before committing. This happened with: Tailscale vs Cloudflare, NanoClaw-centric vs MC-centric todos, delegation vs inject, OneCLI migration.
- **Prefers stepping stones over big jumps**: "I will get there, I just want to take it in steps to minimise breakage" (on upstream merge). Also chose to keep credential proxy now and migrate later, rather than doing everything at once.
- **Documents decisions for future context**: Asked to update CUSTOMIZATIONS.md multiple times unprompted. Also asked for port map, safety hooks, and the OneCLI decision to be documented. Values institutional memory.
- **Wants agents to be transparent about pickup**: "I think I need confirmation it's picked up" — prefers agent sets in_progress (not MC) so there's visible confirmation the agent is actively working.

### Notable Quotes

- "I think proactive AI is what makes it AI native" — on agents suggesting todos from meetings
- "I will get there, I just want to take it in steps to minimise breakage" — on upstream merge approach
- "My concern is if they keep making more changes that is dependant on onecli" — thinks about long-term maintenance cost of diverging from upstream
- "I also know that the tome skills and what not are used by Sky... But do you, Claude Code use it? Should you?" — actively thinks about which agents should share which capabilities

### Mode Transitions

- Session spanned multiple days. Pattern: exploration (API design, todo brainstorm) → implementation (subagent execution) → testing/debugging (container networking, port issues) → maintenance (code review, test suite, upstream merge) → back to exploration (LLM Wiki, ToME improvements)
- Will transitions from building to thinking easily. The LLM Wiki discussion came after a full day of implementation — he shifts to conceptual mode when the building is done.

### Predictions to Test

- H17: Will prefers to keep upstream alignment even at cost of extra work during merges — he expressed concern about OneCLI drift. Confidence: 70%. Validate: observe next upgrade decision.
- H18: Will values documentation of decisions (not just code) as a first-class artifact. Confidence: 80%. Validate: observe if he asks for decision docs in non-NanoClaw contexts.

---

## Session: Worker containers feature — design, implementation, debugging (Claude Code via Telegram)

### Corrections

- Worker folders used colon in name (`worker:todo-abc`) → Docker `-v` mount syntax uses colons as separators → containers failed with "invalid mode" → Changed to hyphen (`worker-todo-abc`). Learning: filesystem/Docker naming constraints must be considered for any new naming convention.
- Proposed keeping worker folders for "audit trail" → Will: "Wouldn't this create a mess as todo items are completed over time?" → Switched to soft-delete with HITL cleanup. Learning: Will thinks about long-term system hygiene, not just current correctness.
- Worker set status to "completed" directly instead of "awaiting_review" → subagent implementer wrote simplified CLAUDE.md instructions that didn't match the plan → Learning: verify subagent output against the plan spec, especially for prompt/instruction content that shapes agent behavior.
- Multiple `localhost:3004` references silently failing because NanoClaw API binds to Docker bridge (172.17.0.1) → Learning: when debugging "nothing happened", check network reachability first.

### Approvals

- Soft-delete to trash with weekly HITL cleanup: "Yeh that sounds right" — Will preferred the human-in-the-loop confirmation over auto-purge.
- Subtask decomposition for multi-step todos: "That's a good idea" — approved using MC subtasks for visibility and human-dependent step tracking.
- Result_content as running conversation log: "Yes" — immediate approval of append-only threaded format.

### Patterns

- **Safety-first thinking for destructive operations**: Will proactively raised concerns about rm -rf ("What about a safer way?"), suggested the two-step trash approach himself, and asked about path traversal protection. This isn't just "incremental" — it's specifically about protecting against irreversible damage.
- **Wants notification flow through existing channels**: "I'm missing now its notification via sky" — workers should route notifications through the user's existing channel (Telegram), not create new notification paths. The user's attention is on their primary channel.
- **Spots systemic issues quickly**: Will noticed the folder accumulation problem, the missing notifications, and the result_content overwrite issue before I flagged them. He thinks about the system in operation over time, not just the happy path.

### Notable Quotes

- "The problem I think is the fact that the worker folders all persist. Wouldn't this create a mess as Todo items are completed over time?" — thinks about long-term system state
- "I just need to be careful with the rm. In case it deletes folders it's not supposed to." — safety-first instinct for destructive operations
- "It's likely going to take multiple tasks to complete... some tasks might require me. What do you think? What's the better architecture" — thinks architecturally about human-AI coordination
- "I'm missing now its notification via sky, since it's now gone direct to the worker" — immediately notices when a change breaks an existing feedback loop

### Mode Transitions

- Started in implementation mode (clear todo spec, ready to build)
- Design discussion phase: Will asked probing questions about trash cleanup, safety, subtask architecture — explored options before approving
- Implementation: approved subagent-driven execution, let it run
- Debugging: shifted to live testing and iterative fixes when things didn't work in production
- Pattern: Will transitions fluidly between modes — doesn't need explicit prompting

### Predictions to Test

- H19: Will prefers notifications through existing channels rather than new notification mechanisms. Confidence: 70%. Validate: observe next feature that involves alerting the user.
- H20: Will proactively identifies long-term system hygiene issues (accumulation, cleanup, staleness) before they manifest. Confidence: 70%. Validate: observe next feature discussion for unprompted lifecycle/cleanup concerns.

---

## Session: PAT/git setup, container architecture clarifications, wiki design, tome-observe (Telegram Main — evening)

### Questions

- "Is that a secure way to store secrets? Is that how it's done in nanoclaw?" → Security-conscious before accepting a pattern, even one he set up himself. Doesn't assume current approach is correct.
- "Is workspace/global that is something accessible by all agent containers" → Proactively thinking about cross-agent shared state accessibility.
- "What's the difference between groups/global and workspace/global" → Wants to understand architecture conceptually before relying on it.
- "So when you say mount, it's just giving a new path to an existing path somewhere on the machine" → Classic grounding question — confirming his mental model before proceeding.
- "Is it just in the main chat though? There could be multiple interactions across multiple agents" → Immediately spotted the limitation of single-channel tome-observe; thinks about multi-agent state coverage.
- "Do you think the journal entry is the right balance?" → Meta-reflection on whether the system is actually capturing what it should. Monitors quality of his own tools.

### Patterns

- **Understands before trusting**: Asked 4 separate clarifying questions about how mounts/PAT/workspace/global works before accepting it as "the way it's done." Won't proceed on assumption.
- **Multi-agent thinking as default**: Immediately extended the tome-observe problem from "main chat" to "all agents" — this is his natural framing, not prompted.
- **Schema flexibility preference**: On wiki design, pushed back on specific MD files (risk-assessment.md etc.) as "too strict" — wants templates that don't presuppose content type. Preferred: agent-inferred structure based on actual content.
- **Quality monitoring on own systems**: Expressed dissatisfaction that the journal "isn't being updated much" — monitors effectiveness of tools, not just existence.
- **H20 second data point**: Raised journal coverage concern before I flagged it. Pattern of proactively spotting gaps in operational systems. → Update applied 2026-04-05

### Approvals

- Nightly tome-observe scheduled task with script (no wake-up if no messages): "That sounds good. I do like the idea of a script that skips if no messages that day."

### Notable Quotes

- "I just feel like it's not being updated much. But at the same time I don't know if it's missing any signals." — meta-awareness of system limitations; wants signal coverage, not just any journal.
- "Is it just in the main chat though? There could be multiple interactions across multiple agents" — naturally extends single-agent design to multi-agent context.
- "I'm wondering is it too strict? Not all projects will have the same type of content." — on wiki schema rigidity.
- "So when you say mount, it's just giving a new path to an existing path somewhere on the machine" — grounding analogy; confirms understanding before moving on.

### Predictions to Test

- H21: Will prefers adaptive/inferred schemas over predefined rigid templates in knowledge systems. Confidence: 70%. Evidence: wiki feedback (too many specific MD files), ToME's flexible journal structure approved. Validate: next time a schema/template is proposed, observe pushback on rigidity.
- H22: Will is security-conscious about secrets storage even in trusted contexts. Confidence: 75%. Evidence: questioned PAT-in-file approach explicitly. Validate: next secrets/credential discussion.

---

## Session: AiD COO delegation — operations hub ingestion (Telegram Main — evening)

### Patterns

- **Delegation testing**: Asked Sky to "ask aid coo what it knows" twice (first time seemingly failed or was insufficient). Wanted to verify the specialist agent's knowledge state before relying on it.
- **Operational knowledge handoff**: Shared detailed AiD Operations Hub snapshot with specific revenue, margins, timelines — treats Sky as a capable operations proxy.

### Questions

- "What's the status of Alceon?" → Testing whether ingested knowledge was queryable immediately. Validates the ingest → query loop.

### Predictions to Test

- (No new hypotheses — consistent with existing pattern of verifying system state before relying on it)
